function getInputValueById(id) {
    const inputNumber = parseFloat(document.getElementById(id).value);
    return inputNumber;
}

function getTextFieldValueById(id) {
    const inputNumber = parseFloat(document.getElementById(id).innerText);
    return inputNumber;
}